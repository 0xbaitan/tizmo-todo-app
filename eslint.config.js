// eslint.config.js - ESLint v9+ flat config
// Note: apps/ folder is excluded - each app manages its own linting
export default [
  {
    ignores: ['apps/**']
  },
  {
    rules: {
      // Add custom rules here
    }
  }
]
